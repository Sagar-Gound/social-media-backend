import { Router } from 'express';

const router = Router();

// Placeholder — add post CRUD routes here
// e.g. GET /posts, POST /posts, GET /posts/:id, PATCH /posts/:id, DELETE /posts/:id

export default router;
